{{ toCsharpComment description }}
public class {{ name }}{{#if hasInterfaces}} : {{#each interfaces}}I{{this}}{{#unless @last}}, {{/unless}}{{/each}}{{/if}} 
{
{{#each fields}}
  {{ toCsharpComment description }}
  public {{ convertedType this }} {{toPascalCase name }}{{ getOptionals this }} { get; set; } 
{{/each}}
}