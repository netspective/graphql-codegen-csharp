{{#each interfaces}}
  {{~> interfaces }}

{{/each}}
{{#each types}}
  {{~> classes }}

{{/each}}
{{#each inputTypes}}
  {{~> classes }}

{{/each}}
{{~#each types}}
  {{~#each fields}}
    {{~# if hasArguments }}
public class {{ toPascalCase name }}{{ toPascalCase ../name }}Args 
{
{{#each arguments}}
  {{ toComment description }}
  [JsonProperty("{{ name }}")]
  public {{ convertedType this }} {{ toPascalCase name }}{{ getOptionals this }} { get; set; } 
{{/each}}
}

    {{/if}}
  {{/each}}
{{/each}}
{{#each enums}}
  {{~> enum }}
{{/each}}
{{#each unions}}
{{ toComment description }}
public class {{ name }} = {{#each possibleTypes}}{{this}}{{#unless @last}} | {{/unless}}{{/each}};

{{/each}}
