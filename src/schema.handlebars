{{#each scalars}}

{{ toComment description }}
public {{ name }} {{ name }} = new {{ name }}();
{{/each}}
{{#each interfaces}}
  {{~> interfaces }}

{{/each}}
{{#each types}}
  {{~> classes }}

{{/each}}
{{#each inputTypes}}
  {{~> classes }}

{{/each}}
{{~#each types}}
  {{~#each fields}}
    {{~# if hasArguments }}
public interface {{ toPascalCase name }}{{ toPascalCase ../name }}Args {
{{#each arguments}}
  public {{ convertedType this }} {{ name }}{{ getOptionals this }}; {{ toComment description }}
{{/each}}
}
    {{/if}}
  {{/each}}
{{/each}}
{{#each enums}}
  {{~> enum }}
{{/each}}
{{#each unions}}
{{ toComment description }}
public class {{ name }} = {{#each possibleTypes}}{{this}}{{#unless @last}} | {{/unless}}{{/each}};

{{/each}}
